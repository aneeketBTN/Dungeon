{
  "name": "dungeon-access-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:standalone": "node scripts/build-standalone.mjs",
    "check": "node ../tools/build_written_authority_assets.mjs && wrangler deploy --dry-run --outdir dist",
    "types": "wrangler types"
  },
  "dependencies": {
    "jose": "6.2.8"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "0.28.1",
    "wrangler": "4.120.1"
  }
}

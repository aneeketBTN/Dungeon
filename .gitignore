# Secrets and local environment
.env
.env.*
!.env.example

# Dependencies and generated deployment output
node_modules/
dist/
*.tgz
.wrangler/
cloudflare/worker-configuration.d.ts
cloudflare/.generated/
cloudflare/dist-standalone/

# Real learner data and local runtime state
state/
history/
leaderboard.json
leaderboard.json.tmp

# Owner-supplied or generated working material
# Directory-name patterns (no leading path) so a future move cannot escape them.
CLAs/
outputs/
work/
tunnel-out.txt

# Private transfer and coordination records
_TRANSFER/
coordination/exchange/

# OS/editor noise
.claude/settings.local.json
.DS_Store
Thumbs.db
*.pyc
__pycache__/

# Agent worktrees: checkouts of other branches living inside this one.
.claude/worktrees/

# Course CLAs — owner-supplied reference material, not for commit.
docs/course-material/

# The teaching layer is complete — 283 lectures, 283 taught — and module 4 held two more composites

`VERIFIED(REAL_BROWSER + AUTOMATED)` — 2026-08-19, branch `fix/theme-switch-and-login-theming`.
Not merged, not deployed.

| lecture | chars | what happened |
| --- | --- | --- |
| `SPMS-M04-L03` — Pricing Strategies | 10,052 | **authored** |
| `SPMS-M04-L05` — Pricing Diagnosis | 20,297 | **authored** |
| `SPMS-M04-L06` — Revenue Models | 20,572 | **authored** |
| `SPMS-M04-L08` — Financial Management and Forecasting | 8,516 | **authored** |
| `SPMS-M04-L10` — Guest Session 02 (Sriraman) | 48,232 | **authored** — the longest lecture in the course |
| `SPMS-M04-L04` — Pricing as Business Lever | — | **composite rewritten** |
| `SPMS-M04-L09` — Funding Considerations | — | **composite rewritten** |
| `SPMS-M04-L07` — Unit Economics | — | gained three terms that were never `L09`'s |

Registered entries **278 → 283**; SPMS **79 → 84 of 84**; backlog **5 → 0**.

## Every lecture in the course now has a lesson

```
BRGSA: COMPLETE — all 50 lectures have lessons
IBM:   COMPLETE — all 78 lectures have lessons
SCLM:  COMPLETE — all 71 lectures have lessons
SPMS:  COMPLETE — all 84 lectures have lessons
```

283 lectures; 283 registered entries — 282 lessons plus one add-in, a lecture folded into a
neighbour's lesson and registered so every gate counts it as taught.

**What this is and is not.** It closes the authoring backlog opened when the teaching layer was
started, and it is *not* the same as the course being tested: 184 of the 283 entries are still
readable-only because no scored question cites their lecture. That is Phase 2's job, sized in
`docs/briefs/TESTING_LAYER_BUILD_PLAN.md`. Content also remains owner-accepted rather than
faculty-reviewed.

---

## Two composites, both already sitting unread in the sweep's own queue

Module 4 held the last two, and unlike every earlier case **both had positive margins** — a rival
genuinely beat them — so the *original* Step 4c query would have caught either. Both had been in
the sweep's output since it was first run this session and neither had been read.

| lesson | ownLift before → after | margin before → after | rival before → after |
| --- | --- | --- | --- |
| `SPMS-M04-L09` | **0.181 → 0.456** | **+0.024 → −0.337** | `M04-L06` → `M01-L08` |
| `SPMS-M04-L04` | **0.204 → 0.467** | **+0.026 → −0.271** | `M04-L03` → `M04-L03` (0.196 vs own 0.467) |

**`SPMS-M04-L09` announced itself in its title,** like `M08-L08` before it: it was called *"Revenue
models, funding stages, and forecasting"* while its lecture is **Funding Considerations**. Revenue
models is `L06`, forecasting is `L08`, and unit economics — its entire second paragraph — is `L07`.
It borrowed from **three** lectures, and one of them (`L07`) was already authored, so it was
duplicating a neighbour rather than filling a gap. Only its third paragraph was its own.

**`SPMS-M04-L04` borrowed from an authored neighbour too, and the gate pointed elsewhere.** Two of
its three paragraphs were the Spotify tier analysis and the monetise/subsidise rule — measured to
`SPMS-M04-L01`, which carries `Spotify` 28 times, `emotional value` 6 and `ecosystem value` 2,
against **0 of each in `L04`'s own transcript**. `M04-L01`'s lesson already teaches all of it,
richly, from its own lecture.

> **A limit of the match gate worth recording.** `L04`'s flagged rival was `M04-L03`, not `M04-L01`.
> The rival is where *vocabulary* overlaps most, which was `L03`'s pricing-strategy language in
> `L04`'s one genuinely-own paragraph — not where the text was actually taken from. **The rival
> names a neighbour, not necessarily the source.** Reading the lecture is what located `L01`.

### Nine for nine: the backlog caused the composites

Every confirmed composite in the record borrowed from a lecture that had no lesson at the time it
was written — `M05-L06`, `M02-L07`, `M07-L08`, `M03-L08`, `M06-L09`, `M06-L01`, `M08-L08`,
`M04-L04`, `M04-L09`. **The defect is not incidental to the backlog; it is produced by it.** An
author facing an unwritten neighbour absorbs its material rather than leaving a gap.

Two of the nine — `M04-L04` and `M04-L09` — also duplicated *authored* neighbours (`M04-L01`,
`M04-L07`), so the mechanism is not purely gap-filling. But with the backlog now at zero, the
condition that generated all nine no longer exists for new authoring.

---

## Coverage: seven terms rehomed, three of which were never where they sat

Rewriting the two composites stripped seven tracked or near-tracked terms. Each was rehomed **in
the same edit**, so no ratchet fired:

| term | from | to | why |
| --- | --- | --- | --- |
| `one-time charge` | `L09` | **`L06`** | Revenue Models is its lecture (1 occurrence; 0 in L09) |
| `subscription` | `L09` | **`L06`** | 15 occurrences in L06 |
| `payback period` | `L09` | **`L07`** | 6 occurrences in Unit Economics; 1 in L09 |
| `gross margin` | `L09` | **`L07`** | 5 in L07 |
| `churn rate` | `L09` | **`L07`** | churn appears 9× in L07 — see below |
| `penetration pricing` | `L04` | **`L03`** | Pricing Strategies names it (2 occurrences) |
| `superforecasting` | `L09` | **`L08`** | Tetlock's *super forecasting* is in L08's transcript, not L09's |

**Three phrases occur nowhere in the SPMS corpus at all** — `funding stages`, `superforecasting`
and `churn rate` — joining `AI bias`, `Requirement lifecycle` and `Team roles`. They are
syllabus-sheet phrases the lectures never say (the course says *super forecasting* with a space,
and *churn* without *rate*), and they pass LAW-49 through the module notes rather than the
transcripts. They must stay present in some lesson's text or the syllabus ratchet fires, so each
sits at the lecture that teaches the idea.

**A standing warning was cleared as a side effect.** `L04` glossed `liquidity pricing`, which was
one of the nine long-standing vocabulary warnings — *"appears in neither the SPMS transcripts nor
the module's notes"*. It was invented, it is gone, and the bank validator now reports **8** warnings
rather than 9.

---

## Scores

| lesson | ownLift | margin | nearest rival |
| --- | --- | --- | --- |
| `SPMS-M04-L03` | **0.538** | −0.384 | `M04-L04` |
| `SPMS-M04-L05` | **0.536** | −0.441 | `M04-L01` |
| `SPMS-M04-L06` | **0.496** | −0.362 | `M08-L07` |
| `SPMS-M04-L04` *(repaired)* | **0.467** | −0.271 | `M04-L03` |
| `SPMS-M04-L09` *(repaired)* | **0.456** | −0.337 | `M01-L08` |
| `SPMS-M04-L10` | 0.427 | −0.394 | `M05-L02` (0.034) |
| `SPMS-M04-L08` | 0.394 | −0.304 | `M04-L06` (0.090) |

Final corpus calibration across **283** lessons: p05 0.328, p25 0.471, p50 0.553, p75 0.626.

Five of seven clear p25. `L10` and `L08` fall below with very wide margins — rivals at 0.034 and
0.090 — and both fit patterns already recorded: **`L08` is a short lecture (8,516 chars) inside a
vocabulary-dense neighbourhood**, and **`L10` is a 48k conversational guest session** whose
vocabulary is unusually broad, which dilutes distinctiveness without any rival coming close. Neither
meets the sweep's condition.

**LAW-76 diff: zero untouched lessons moved by ≥0.02.** Fourth consecutive batch.

The guest session's explainer came in at **326 words** and was flagged by `check_lesson_file`;
trimmed to **296** (paragraphs 641/593/628 chars) and re-verified.

---

## Gates

| gate | result |
| --- | --- |
| `check_lesson_file.mjs "<transcripts>"` | `ok: true`, **0 errors**, **all four subjects COMPLETE** |
| `validate_t6_bank.js "<transcripts>"` | `ok: true`, **0 errors**; 283 authored; **8 warnings, down from 9** |
| `npm run check:syllabus` | **PASS** — SPMS 116/116, all four 100% against 100% floors |
| `npm run check:taught` | **PASS** — no new untaught vocabulary |
| `npm run check:tested` | **PASS** — all four at floor |
| `check-lesson-lecture-match --gate` | **FAIL naming `SPMS-M01-L01` and nothing else** — the expected state |
| `npm test` | **128 / 128** |
| `node tools/build-site.mjs` | 19 assets |
| `tools/browser-checks/teach-before-test.js` | **`ok: true`, 12 routes × 4 subjects, 0 violations, 0 skipped** |
| `node tools/screenshot.mjs --port 8099` | **16 / 16**, 0 failed |

### Browser verification

283 lessons loaded in the running app at `http://127.0.0.1:8099/app/t6.html`. All seven new and
rewritten titles render; **both stale composite titles are gone** (`"Pricing as strategy: the
Spotify case"`, `"Revenue models, funding stages, and forecasting"` both return false). **Zero
mojibake and zero literal-markdown sequences across the whole lesson index** — the two Step-5-only
defect classes found earlier today were both scanned for explicitly. Console carries no JS errors;
the only failing request is `/api/written-authority/health` → 404, the hosted-marking probe the
static dev server does not implement.

### Chain

All ten module 4 handoffs read correctly. Two were repaired: `L04` had promised revenue models
(`L06`) and skipped `L05`, and `L09` had claimed to close the module with `L10` still to come —
the same close-of-subject falsehood found in `M08-L08`. `L09` now hands to the guest session and
`L10` hands to module 5.

---

## What is now open

- **Testing, not teaching.** 184 entries are readable-only. The queue and its costs are in
  `docs/briefs/TESTING_LAYER_BUILD_PLAN.md`: in scope (IBM descoped), 269 ideas, 109 reached,
  35 drift, 98 partial, **27 genuine zeros**.
- **The importance ranking still wants an owner pass** before it orders ~3,000 questions.
- **`SPMS-M01-L01` remains the one expected match-gate failure**, by owner decision.
- Content remains **owner-accepted, not faculty-reviewed**.
